// Import Styled Components
import { StyledFooter } from './_footerStyle';

function Footer() {
  return (
    <StyledFooter>
      <span>Footer</span>
    </StyledFooter>
  );
}

export default Footer;
