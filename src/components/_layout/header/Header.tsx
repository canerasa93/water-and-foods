// Import Styled Components
import { StyledHeader } from './_headerStyle';

function Header() {
  return (
    <StyledHeader>
      <span>Header</span>
    </StyledHeader>
  );
}

export default Header;
